---
description: 实验执行期的止损约束：每轮先指出会改变哪条命令，超预算自动停
alwaysApply: true
---

# 实验止损

`experiment-driven-doc` 的决策价值门只在开跑前问一次，而偏离是执行中一步步发生的——
每一步都在「把某个数解释干净」，每一步看着都合理。本规则约束执行中的行为。

## 开跑前：指出一条具体命令

**这轮的结果会改变接下来要执行的哪条命令？** 写出那条命令。

不接受的回答：加深理解、排除一个候选、确认某个猜测、为下一轮做准备。
排除候选只有在「排除后下一条命令不同」时才算数。

同一个问题连续两轮答案相同 → 这条线零信息，进 backlog，不再续。

## 预算：先声明，超了停

开跑前声明轮数与机时上限（默认 2 轮 / 2 小时 GPU）。超了**自动停并上报**，
不允许「再一轮就好」。沉没成本不是续的理由——跑完还要付读结果、写结论的 token。

## 每轮结束：报改变了哪条命令，不报读数

读数、候选集、solver 清单都不是产出，它们指向的那条「下一步命令变了」才是。
报不出来就明说这是零信息轮。

## 选项不能全是同一类

给用户的选择题里，若三个选项都是当前路径的不同变体，那就是把偏离包装成了选择。
至少留一个「停下来回主线」的选项。

## 硬性中止

任一命中即停，写清当前位置与回主线的第一步：连续 2 轮结果无论正负都不改变主线／
连续 2 轮同一类环境错误／超预算／发现主线其实没被这个问题卡住。
