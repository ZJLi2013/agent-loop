---
description: 实验执行期的止损约束：每轮先指出会改变哪条命令、超预算自动停，以及唯一一份「什么情况才停下来问人」的中止清单
alwaysApply: true
---

# 实验止损

`experiment-design` 的决策价值门只在开跑前问一次，而偏离是执行中一步步发生的——
每一步都在「把某个数解释干净」，每一步看着都合理。本规则约束执行中的行为，
并持有**唯一一份中止清单**（别处只留指针）。

## 开跑前：写出两条具体命令

**结果为真时执行哪条命令，结果为假时执行哪条命令。** 两条都写出来，且它们不一样。

不接受的回答：加深理解、排除一个候选、确认某个猜测、为下一轮做准备。
排除候选只有在「排除之后那条命令确实换了」时才算数。

同一个问题连续两轮答案相同 → 这条线零信息，降优先级回 `task.md`，不再续。

## 预算：先声明，超了停

开跑前声明轮数与机时上限（默认 2 轮 / 2 小时 GPU）。超了**自动停并上报**，
不允许「再一轮就好」。沉没成本不是续的理由——跑完还要付读结果、写结论的 token。

## 每轮结束：报改变了哪条命令，不报读数

读数、候选集、solver 清单都不是产出，它们指向的那条「下一步命令变了」才是。
报不出来就明说这是零信息轮。

## 选项不能全是同一类

给用户的选择题里，若三个选项都是当前路径的不同变体，那就是把偏离包装成了选择。
至少留一个「停下来回主线」的选项。

## 先自修，再中止

**「遇到非预期情况」不等于「问人」。** 下面三类由 agent 自己处置完，才轮到中止清单：

| 类别 | 处置 |
|---|---|
| 环境 / 瞬时错误（OOM、认证超时、缺依赖、端口占用、磁盘满、架构不匹配） | 自己修，同类最多 2 次并计数；远端 GPU 的具体修法见 `remote-exec` |
| 契约不匹配（维数、单位、枚举名、帧数） | 按 `experiment-design` 的「层 0 复现对方样例」定位 |
| 假设被推翻 | **这是结果不是故障**，回填文档然后重排优先级，不上报 |

只有**重试上限用尽**后才升级为 blocker，上报时带上已试过哪几条。

## 硬性中止

任一命中即停，写 `## Auto-Stop Report`：当前位置、已试过什么、回主线的第一步。

1. 连续 2 轮结果无论正负都不改变主线
2. 连续 3 轮指标无改善或只在 noise 内波动
3. 连续 2 轮同一类环境错误且自修已用尽重试上限
4. 超预算（轮数或机时）
5. Phase 0 假设可能不成立，或发现主线其实没被这个问题卡住

另有三类与实验轮次无关、无论何时都要停下等人：**不可逆操作**（删数据、覆盖 checkpoint、
强推分支、长时间独占 GPU）、**Goal 说不清**（写不出一句话的「要解决什么问题」）、
**需要用户在有显著权衡的方案间选型**。
