---
description: 新增或修改 SKILL.md 时的体量与内容约束，防止 skill 文档膨胀
globs: ["**/SKILL.md", "**/.cursor/skills/**/*.md"]
alwaysApply: false
---

# SKILL.md 写作约束

skill 的成本是 context：**description 每轮对话都加载，正文一旦命中就全文加载**。约束按这两档分。

## description —— 每轮都付费

三到五行封顶：做什么 + 什么时候用（英文 trigger phrase 要具体）+ 与相邻 skill 的边界。
不写步骤、不写实现细节，那是正文的事。

## 正文 —— 命中才付费，但一次全量

- **软上限 150 行。** 超了先砍；砍不动再拆 `reference.md` / `scripts/`。
- **只写 agent 自己想不到的**：判据、阈值、禁止操作、踩过的坑、非直觉的执行顺序、外部系统的真实行为。
- **不写 agent 自己能生成的**：标准 bash/python 样板、逐行解释、教科书式概念说明。
- 每加一节先问：**读者会因为这节改变行动吗？** 不会就删。

## 三种反复出现的膨胀

| 症状 | 处理 |
|---|---|
| 同一命令/模式换个场景贴 3–5 遍 | 留一份规范写法，其余变体各一句话 |
| 一句话能说清的规则写成 40 行脚本 | 保留判据、阈值、禁令，删掉可自动生成的样板 |
| 示例把所有实例列全（5 个节点、9 个 tag） | 一个实例 + 字段含义 |

## 更新是替换，不是追加

- **一个知识点只归属一个 skill / rule，别处只留一句指针**（`见 <名> 的「<段名>」`）。
  这是本库最容易破的一条：同一条判据在三处各写一遍，改的时候只改得动一处。
- 改完对比行数：**比改之前长了，就要能说出新增的是哪条决策**；说不出就是在追加而不是替换。
- 新结论覆盖旧措辞的写法归 `narrative-spine` 的「直接陈述当前为真的内容」。

## 自检

- 只读前 10 行，能不能说出这个 skill 何时该用、硬约束是什么？
- 每节篇幅与它对 agent 行为的影响成正比吗？
- 有没有和别的 skill 重复的段落？
