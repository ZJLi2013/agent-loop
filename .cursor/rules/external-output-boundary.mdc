---
description: 跨出自己仓库边界的产出（他人仓库 PR/Issue/评论、对外博客）必须过的三道检查：AI 披露、型号脱敏、私料清理
alwaysApply: true
---

# 对外产出的边界检查

**这条边界的成本不对称**：一次没披露、没读懂、带着私料的提交，代价由维护者或雇主承担，
而且会记在提交者名下。本地怎么用 AI、内部文档写什么都不受此约束，**跨出仓库边界才受**。

三道检查在同一刻生效，缺一不可：**披露**、**脱敏**、**去私料**。
PR / Issue body 的具体模板见 `upstream-contribute`，博客的谋篇与去 AI 味见 `research-to-blog`。

## 一、AI 披露

**先读目标项目自己的 AI 政策**（`AI_POLICY.md` / `CONTRIBUTING.md` / PR 模板），它优先于本规则。
下面是没有明文政策时的默认线，取自 [Ghostty AI Usage Policy](https://github.com/ghostty-org/ghostty/blob/main/AI_POLICY.md)——
Ghostty 会把违反者列进公开的 denouncement list 并永久拒收。

| | 线 |
|---|---|
| 披露 | 说明用了哪个工具（Claude Code / Cursor / Amp…）**以及 AI 参与到什么程度**。"AI-assisted" 一词不够 |
| 理解 | 提交者要能**脱离 AI** 讲清这段改动做什么、怎么与系统其余部分交互。讲不清就不提 |
| 编辑 | Issue、讨论、PR 正文可以用 AI 起草，但必须由人**审阅并删改**后再提。AI 天然啰嗦，噪声要人来砍 |
| 媒体 | **只允许文本与代码**。不提交 AI 生成的图、视频、音频 |

## 二、型号脱敏

**所有对外内容（PR body、Issue body、commit message、博客）中，必须把中国大陆特供 GPU 型号
替换为北美对应版本号。** 实验文档内部可以保留真实型号；生成对外产物时**自动替换，不等用户提醒**。

| 大陆型号 | → 替换为 |
|---|---|
| MI308X / MI308XHF | MI300X |
| MI308 | MI300 |
| MI258 | MI250X |
| MI108 | MI100 |

Docker image tag、ROCm 版本号不受影响。遇到表上没有的大陆型号，**警告用户确认**对应北美型号，
不要自己猜。

## 三、去私料

对外产物里**不出现**：本地文档路径引用（`见 docs/study.md §8` 这类）、内部 repo 名、
公司 / 集群 / 节点主机名、内部 issue 号、未公开的性能基线。

需要承接内部内容时，做法是把内部洞见**改写**进对外稿，而不是链接内部文件。
实验记录（`experiments.md`、`overnight-results/`）不进 PR 分支。

## agent 侧的执行

- 生成 PR / Issue / 博客时**主动带上披露段并完成脱敏**，不等用户提醒；工具名与参与程度按实情写，
  不含糊成「部分借助 AI」。
- 交付前**明确提示这份草稿需要人过一遍**，并指出哪几段是 AI 判断、最该被核对。
- **提到他人仓库的产物里一律不放生成图**（这是上表「媒体」那条：代价由维护者承担）。
  需要图就用真实产出——trace、曲线、截图；结构图用 mermaid / ascii。
  自己的博客 / 公众号封面可以用生成图，约束见 `research-to-blog`。
- 语态（第三人称、不 overclaim、直接陈述真值）归 `write-for-humans`。

## 边界

只管**对外**产物：他人仓库的 PR / Issue / 评论、对外博客、公众号。
自己的 repo、实验记录、内部文档不受约束。
