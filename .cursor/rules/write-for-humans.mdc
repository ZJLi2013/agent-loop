---
description: 一切给人看的产出（对话回复、文档、PR、issue）：结论先行、一条能被复述的主线、不为解释而膨胀、直接陈述当前为真的内容
alwaysApply: true
---

# 写给人看

**一段回复、一篇文档、一个小节，都只服务一个能被复述的判断。** 其余内容是它的证据、边界或
副产物，结构上低一级。写全、写细不等于写清楚——**多写的每一句都在稀释真正要被记住的那一句**。

判据：**human 不该承担阅读负担**。读者读完说不出主线是什么，或者要往回翻才能确认某段在讲什么，
这一份就不合格。

## 结论先行

- 第一句就是结论，且能独立成立；证据、边界、方法一律排在它后面。
- **读数、清单、候选集本身不是结论**；它们指向的那一句判断才是。
- 用户反问「所以结论是什么」= 上一条回复不合格。
- 一问一答直接给答案，不加背景铺垫，不附赠下一步建议。
- 不确定就写「不确定 + 缺什么才能确定」，不用长段落对冲。
- 被问真伪（「是不是 X？」）时先答「是 / 否」，再给真值。

## 解释性膨胀

**最常见的失手不是啰嗦，是为了解释而解释。** 引用一个读数、概念或机制时，把它的**推导**也搬进
正文——每一句看着都在「说清楚」，合起来把结论埋掉了。

| 症状 | 处理 |
|---|---|
| 提到「71% 的样本没梯度」，就展开 GRPO 组内标准化、回报取值、筛除条件 | 只写它指向的那句判断，推导留在它的出处并链接 |
| 为一个次要术语补一段定义 | 一个从句，或不写 |
| 「先把这个数说清楚」「为完整起见」开头的段落 | 删 |
| 同一个机制在两节里各讲一遍 | 留在主线经过的那一节 |

自查：**这段整段删掉，读者的下一步动作会变吗？** 不会就删。
**读者主动问「这个概念是什么」时才展开**——那时它是被问的对象，不是膨胀。

## 只答被问到的那个对象

问 feature16 的配置就只给 feature16 的数。**引入邻近对象（另一份 recipe、历史读数）来「对照」
「佐证」「顺带说明」，就是在分散注意力**，读者要多分辨一次哪个数属于哪个对象。

- 需要区分同名的多个数时，只写被问的那个 + 一句它的定义，不列全表。
- 邻近对象只在**它改变结论**时出现（例：这个数在两处不一致，取哪个会改答案）。
- 「顺带一提 / 另外值得注意」开头的句子，删。

## 组织

- 组织轴是**重要性**，不是执行顺序，也不是自己发现的顺序。
- 标题写**结论本身或问题本身**，读者在读正文之前就该懂它。要读完才知道指什么的标题
  （「那 X 是同一件事的另一种说法」）、以及写它在流程里角色的标题（「多出一条前置修复」），
  都是零信息。
- **一组独立的事实写成清单，不连成段落。** 现状、影响、收益、改法各自成条，读者能挑着读；
  连成散文就得整段读完才知道哪条是要做的事。

## 直接陈述当前为真的内容

**先写出那个真值，再（仅在必要时）用一个从句纠偏。** 「不是 X，是 Y」「原先以为 X 其实是 Y」
这类句式一律改写——读者要先在脑中构造 X 再删掉它，才拿到真正的答案。**X 往往只存在于用户的
猜测、本文档的旧版本或上一轮对话里，它不该占用读者的工作记忆。**

| 写成了 | 应该写 |
|---|---|
| 不是 32，是 16 | 每卡并发 16 条 env，一次 forward 的 batch 就是 16 |
| 结果推翻了先前的直觉排序：最大一块不是 RoPE 而是 paged-KV 的 78 ms | 最大一块是 paged-KV 的写入与索引簿记：78 ms |
| 这一轮把它从「已知该做」变成「已定价 30 ms」 | 这条 TODO 现在有了定价：30 ms |

**但保留「关于世界的否定发现」。** 被测系统的否定事实（「aiter 没有 AdaLN 形态的 kernel」
「假设不成立，RMS 0.05」）是产出，往往还是最贵的那部分；被否决的候选写进专门的
「排除 / 不做」表当长期决定。删掉的只是对自己旧措辞的修订说明。

**去掉对话人称、元叙述与指向文档自身的引用。** 用第三人称（「trace 显示…」），不写「我们发现」
「值得注意的是」「要我继续吗」。更隐蔽的一类是**用规划史代替事实**——「真正挡路的是组身份，
featureN 的边界里明确 scope out 了」：读者要的是那个问题是什么、怎么改。
**判别：删掉句子里的文档名、轮次号、planning 动词，剩下的实质内容变少了吗？** 没变少就别写。

自检 grep（标题与摘要行优先改，它们被读到的次数最多）：

```text
其实|原来|先前|之前以为|推翻|纠正|不再是|我们发现|可以看到|值得注意的是|scope out|前置修复|多出一条
```

## 说「做不到」之前先查

**「做不到 / 会坏掉 / 不成立」会让用户放弃一条可行路，所以它比肯定结论需要更强的证据。**

- 断言前查实现，不停在配置项与调用条件上；再 grep 一遍仓库文档与上游 issue/PR。
- 查不动写「未核实 + 缺什么才能确定」，不写「不成立」。
- **结论里出现「要改代码 / 做不了」时，先确认那件事是不是已经做完了**——按机制名搜一次即可。

## 自检

只读前 10 行能复述主线吗？删掉任一非主线章节，主线还成立吗（成立说明它该降级）？
每节篇幅与它对读者决策的影响成正比吗？

## 边界

用户明确要求「详细展开 / 完整列出 / 给我背景」时才铺开。
**本规则是语态与结构的唯一归属处**，各处只留指针：exp / feature 文档的章节模板在
`experiment-design`；对外博客的去 AI 味清单在 `research-to-blog`；SKILL.md 与 rule 的体量
约束在 `authoring`。
