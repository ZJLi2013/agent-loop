---
description: 多步任务的控制环：先定位当前处在循环的哪一格，再去读那一格对应的 skill；失败按类别自修，不默认问人
alwaysApply: true
---

# Agent Loop

**任何需要多步才能完成的工作，都走这个环，不要每步临场决定下一步。**

```text
Goal ─► PLAN ─► SELECT ─► DESIGN ─► EXECUTE ─► EVALUATE ─┬─ pass ─► CLOSE ─┐
                  ▲                                       │                │
                  │                                       └─ fail ─► DIAGNOSE
                  │                                                   │
                  └──────────── 下一个 task ◄── REPAIR / RE-PLAN ◄─────┘
                                     │
                            task 全部通过 ─► SHIP
```

## 路由：这一格读哪个 skill

**冷 skill 也要读。** `disable-model-invocation: true` 只表示模型不会自作主张地触发它，
被本规则点名时照常读它的 `SKILL.md`。

| 当前在做什么 | 读 |
|---|---|
| user 给了新目标，还没有 `task.md` | `feature-dev-pipeline` — 拆子任务、排 P0/P1/P2 |
| `task.md` 有未完成项，要挑下一个 | `task-loop` — 选取规则与验收检查点的写法 |
| 要跑一轮实验或验证 | `experiment-driven-doc` — 先写假设/预期/判据，再跑 |
| 要在远端或 GPU 上执行 | `remote-exec` |
| 单条命令预计超过 60 秒 | `agent-heartbeat` |
| task 全部通过，要交付 | `code-review` → `upstream-contribute` |
| 调研阶段要产出对外文章 | `research-to-blog` |

`task.md` 是任务状态的唯一真相源。**不要另建 `tasks.json` 或第二份 backlog。**

## 失败：先分类，再决定要不要问人

**「遇到非预期情况」不等于「问人」。** 先判断失败属于哪一类：

| 失败长什么样 | 怎么走 |
|---|---|
| 环境 / 瞬时：OOM、认证超时、缺依赖、磁盘满、架构不匹配 | `remote-exec` 的「失败处置表」自修，每类各有重试上限 |
| 契约不匹配：维数、单位、枚举名、帧数对不上 | `experiment-driven-doc` 的「层 0 复现对方样例」 |
| **假设被推翻** | **这是结果不是故障。** 回填实验文档 → 回 `task.md` 重排优先级，继续 |
| 判据失效：代理指标判不出东西 | `experiment-driven-doc` 的「验收判据阶梯」，往任务指标那层退 |
| 上面都试过且重试上限用尽 | `experiment-budget-gate` 的中止清单——**到这一步才轮到停下问人** |

自修过程要记进 `task.md` 的失败计数，否则分不清「第一次失败」和「已经试了三次」。

## 什么时候停下等人

判据只有一份，在 `experiment-budget-gate` 的「先自修，再中止」与「硬性中止」。
本规则不重述，只强调顺序：**分类 → 自修到上限 → 才查那份清单。**

Goal 本身由 user 定：写不出一句话的「要解决什么问题」就停下问，不要自己替 user 定义。

## 边界

一问一答、单文件小改、纯查询不走这个环——它的成本只在多步任务上才回本。
判断依据：**这件事需要保留跨回合的状态吗？** 不需要就直接做。
