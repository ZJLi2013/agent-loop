---
description: 多步任务的控制环：先定位当前处在循环的哪一格，再去读那一格对应的 skill；失败按类别自修，不默认问人
alwaysApply: true
---

# Agent Loop

**任何需要多步才能完成的工作，都走这个环，不要每步临场决定下一步。**

```text
Goal ─► PLAN ─► SELECT ─► DESIGN ─► EXECUTE ─► EVALUATE ─┬─ pass ─► CLOSE
                  ▲        │                             └─ fail ─► DIAGNOSE ─► REPAIR / RE-PLAN
                  │    hook 注入 INDEX.md                                          │
                  └──────────── 下一个 task ◄───────────────────────────────────────┘
                  task 全部通过 ─► DONE（报告并停）

  .cursor/memory/ 横穿每一格，不是其中一格：要把一个具体值放进命令就读 facts.md，
  第二次用到就写回；转换发生时（task 变绿、假设被推翻、方案被否决）写 episodes.md。
```

## 循环内：条件满足就读

| 当前在做什么 | 读 |
|---|---|
| user 给了新目标，还没有 `task.md` | `feature-planning` — 拆子任务、排 P0/P1/P2 |
| `task.md` 有未完成项，要挑下一个 | `task-state` — 选取规则与验收检查点的写法 |
| 要跑一轮实验或验证 | `experiment-design` — 先写假设/预期/判据，再跑 |
| 单条命令预计超过 60 秒 | `agent-heartbeat` |

`task.md` 是任务状态的唯一真相源。**不要另建 `tasks.json` 或第二份 backlog。**

具体的值（节点名、容器名、checkpoint 路径、分支、image tag）归 `.cursor/memory/facts.md`，
就地覆盖，准入判据见 `agent-memory`：

- **第二次用到一个值时，写进去。** 第一次是发现它，第二次说明它会反复出现。
- **要把一个值放进命令时，读文件，不从对话里回忆。** 上下文只是缓存，长会话里它会被静默压缩，
  而**你不会知道自己忘了什么**。
- 新 session 开工、以及遇到环境类失败时，验证接下来要用到的那几条。
  **过期的事实比缺失的更坏——它不会报错**，命令照常执行，只是打在了错的机器上。

## 岔出：user 明确要了才走

**`task.md` 清空即 DONE——报告结果然后停**，不要因为「task 做完了」就自动往下走。
下面每项被点名时才读，其余时候不碰：审 diff / PR → `code-review`；提上游 → `upstream-contribute`；
写对外文章 → `research-to-blog`；画 kernel 数据流图 → `code-to-kernel-diagram`。

## 漂移：只有一行在 doing，改动必须让它的检查点变绿

**`task.md` 里有且只有一行是 `🔬 doing`。** 多于一行或一行没有，都说明状态已和实际脱节，
先修文件再继续。跑到一半发现设计有问题时，是否顺着改下去，判据是：

> **我这次改动，会让 `🔬 doing` 那行的验收检查点从不通过变成通过吗？**

答案是「不会」或「不确定」，这次改动就不属于当前行。**在动手之前**按下表处置：

| 情况 | 怎么办 |
|---|---|
| 当前 task 没它就走不下去 | 新增一行，当前行改 `🚧 blocked` 并记下 blocked-by，`🔬` 移到新行；新行绿了立刻移回来 |
| 不影响当前 task | 新增一行排进 P1/P2，**`🔬` 不动，继续手头的事** |
| 连一行都写不清楚 | 说明还没想清楚它是什么，记进实验文档，不动手 |

**blocked 链长度 ≥ 2 就停下问人**——套了两层说明最初的拆解不对。链长直接数 `🚧` 的个数，
不靠回忆。这个判断至少做两次：**改第一个文件之前**，以及每次因失败回写 `task.md` 时。

## 无人值守模式

user 说**「无人看守 / 我下班了 / overnight / 你自己跑完 / 明早看结果」**时进入，直到 user 再次说话。
翻转三个开关：

| 平时 | 无人值守时 |
|---|---|
| 拿不准就 `AskQuestion` | **不问**。选最推荐的那个继续，把候选与放弃项记进文档 |
| 有显著权衡的选型停下等 user | 按 KISS 选，标 `⚠️ 待确认` 留给早上 |
| 被挡住就停下等人 | **跳过这条 task 标 `🚧`，去做下一条**——真停下来等，一整夜就没了 |

**只有当所有剩余 task 都被挡住时才真正停机。** 不可逆操作依然不做，做法是跳过它继续别的。
**环境错误的重试上限不放宽**——放宽只会让一个坏掉的环境烧一整夜。

收工前必须落盘，早上要能只看文件就接手：`task.md` 的状态列与失败计数是最新的，外加一份
`## 夜间执行报告`——做完了哪几条、卡住的分别卡在什么上、每个 `⚠️ 待确认` 的决定、明早的第一条命令。

## DIAGNOSE：先分类，再决定要不要问人

六类失败各自的去向、重试上限、以及唯一一份硬性中止清单都在 `when-to-stop`。
本规则只强调顺序：**分类 → 自修到上限 → 才查那份清单。**

Goal 本身由 user 定：写不出一句话的「要解决什么问题」就停下问，不要自己替 user 定义。

## 边界

一问一答、单文件小改、纯查询不走这个环。
判断依据：**这件事需要保留跨回合的状态吗？** 不需要就直接做。
