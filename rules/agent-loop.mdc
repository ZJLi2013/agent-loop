---
description: 多步任务的稳定内核：按当前状态路由到唯一 policy；失败默认自修，完成必须有独立证据
alwaysApply: true
---

# Agent Loop

**任何需要跨回合状态的工作都走这个环；一问一答、单文件小改、纯查询直接做。**
状态与合法转移以 [`harness/protocol.py`](../harness/protocol.py) 为准：

```text
PROPOSED ─► READY ─► RUNNING ─► VERIFYING ─► VERIFIED
    ▲          │          │            │
    │          └──────────┴──► FAILED ─┘
    │                    human correction
    └──────────── PAUSED ◄──────────────┘
                         budget / disable ─► STOPPED
```

## 按状态路由

| 当前事件 / 状态 | 读 |
|---|---|
| 新 Goal、缺 plan Goal、`📝 proposed`、Goal Review、human 纠偏 | `work-planning` |
| 选择 task、写检查点、维护状态 | `task-state` |
| 已批准 task 要跑实验 / 验证 | `experiment-design` |
| 失败、重试、预算或停机判断 | `when-to-stop` |
| 具体值、旧结论、否决项可能相关 | `agent-memory` |
| 单条命令预计超过 60 秒 | `agent-heartbeat` |

## 硬不变量

- `.agent-loop/task.md` 是 backlog 唯一真相源；不建第二份任务清单。
- 等待 review 时允许「若干 `📝 proposed` + 零行 doing」；否则有且只有一行 `🔬 doing`。
- plan document `Plan Revision`、task `rN` 与 Harness runtime revision 必须一致；review 未批准不执行。
- 改动必须让当前检查点从不通过变成通过；否则先按 `task-state` 改状态，不能顺手做。
- `🚧 blocked` 链长 ≥ 2 停下重拆；失败先按 `when-to-stop` 自修到上限。
- human 只问进展时读状态与 evidence 回答；纠正 Goal / scope / 优先级时先 Harness pause，
  再由 `work-planning` 更新 Goal、RECONCILE tasks、review、resume。
- `task.md` 清空即 DONE：报告并停。review、提上游、写博客等不会自动发生。

## 横穿全环

- memory：具体值第二次用到就写；入命令前从 `facts.md` 读；task 变绿、假设被推翻、方案被否决时
  写 `episodes.md` 并同步 INDEX。
- Harness：项目存在启用中的 `.harness/state.json` 时，耗时或改变验收证据的命令经 runner；
  结束前运行锁定 verifier。stop hook 拒绝未验证、证据 stale 或预算不清的完成声明。

## 无人值守

user 明确说「无人值守 / overnight / 我下班了 / 明早看结果」时，直到 user 再次说话：

- Human Review Gate 按 KISS 自批准，plan document 标 `Plan Review: unreviewed`；
- 不问选择题，记录选中理由与放弃项；有显著权衡时标 `⚠️ 待确认`；
- blocked task 跳过做下一条，全部 blocked 才停；不可逆操作仍跳过；
- 环境重试上限不变；收工把状态、失败计数、待确认项和明早第一条命令写进夜间报告。

## 环外能力

只有 user 点名才读：审 diff / PR → `code-review`；提上游 → `upstream-contribute`；
写对外文章 → `research-to-blog`；画 kernel 数据流图 → `code-to-kernel-diagram`；
远端执行 → `remote-exec`。
