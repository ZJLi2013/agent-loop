---
description: 写 SKILL.md 或常驻 rule 时的准入、体量与归属约束；以及一条约束该放 rule、skill 还是 hook
globs: ["**/SKILL.md", "**/skills/**/*.md", "**/rules/*.mdc", "**/.cursor/rules/*.mdc"]
alwaysApply: false
---

# Skill / Rule 写作约束

成本模型决定一切：**skill 的 description 每轮都加载、正文命中才加载；
rule 如果是 `alwaysApply`，则整篇每轮都加载。** 所以加一条常驻 rule 的门槛，
要比加一个 skill 高得多。

## 先决定层，再决定内容

先按 [`design.md`](../design.md) 判断归属，再问**它要在什么时刻生效**：

| 那一刻 | 放哪 | 为什么 |
|---|---|---|
| 新生命周期状态、事件或不可绕过 guard | **`harness/protocol.py` / Harness** | Kernel 是可执行真相源 |
| 不同项目只需要不同参数 | **config / profile** | 不是新行为 |
| 每一轮都可能需要，且那时不会有 skill 被加载 | **常驻 rule** | 只有它无条件在场 |
| 只在写某类文件时需要 | **glob rule** | 按路径挂载，不占平时的位置 |
| 某个状态下怎么做 | **owner skill** | 命中才付费；不重新定义转移 |
| 平台事件接线 | **adapter / hook** | Cursor 只是一个宿主 |
| **必须强制，不能被跳过** | **Harness guard** | rule 仍然是「让模型自己判断要不要遵守」 |
| 要影响 PR 上跑的那次 review | **`.cursor/BUGBOT.md`** | Bugbot 不读 rules，也不读 skills |

**「写成规则」和「能被执行」是两件事。** 一条要求 agent 自省的约束
（「判断一下要不要查记忆」「留意有没有跑偏」）基本不会触发——那个自省时刻不会到来。
这类要么改写成锚在已有产物上的判据（检查点、正在执行的命令），要么升级成 hook。

## 准入

- **agent 自己想不到吗？** 默认就会做的不写（「写清晰的代码」「注意安全」）。
- **在这个库里踩过吗？** 优先写踩过的坑，不写预防性的通用告诫。
- **删掉它，行为会变吗？** 不会就不加。

## 体量

| | 软上限 | 超了怎么办 |
|---|---|---|
| skill description | 5 行 | 把正文内容搬回正文；边界声明能删就删 |
| SKILL.md 正文 | 150 行 | 先砍，砍不动再拆 `reference.md` / `scripts/` |
| rule | 80 行 | 先砍，砍不动说明它其实是个 skill |

**只写 agent 自己想不到的**：判据、阈值、禁止操作、踩过的坑、非直觉的执行顺序、
外部系统的真实行为。**不写它自己能生成的**：标准样板、逐行解释、教科书式概念说明。

一条 rule 只解决一类时刻的问题。同一份文件里出现两个不相干的「时刻」，拆开。

## 三种反复出现的膨胀

| 症状 | 处理 |
|---|---|
| 同一命令/模式换个场景贴 3–5 遍 | 留一份规范写法，其余变体各一句话 |
| 一句话能说清的规则写成 40 行脚本 | 保留判据、阈值、禁令，删掉可自动生成的样板 |
| 示例把所有实例列全（5 个节点、9 个 tag） | 一个实例 + 字段含义 |
| 尾部的「Key Principles / 原则」小节 | 八成在复述本文正文，加之前先问它有没有新增决策 |
| 解释这条规则自己为什么这么设计 | 删。「关于世界的因果」留下（放宽重试上限会让坏环境烧一整夜），它帮 agent 外推 |

## 更新是替换，不是追加

- **一个知识点只归属一个 skill / rule，别处只留一句指针**（`见 <名> 的「<段名>」`）。
  这是本库最容易破的一条：同一条判据在三处各写一遍，改的时候只改得动一处。
- 改完对比行数：**比改之前长了，就要能说出新增的是哪条决策**；说不出就是在追加。
- 新结论覆盖旧措辞的写法归 `write-for-humans` 的「直接陈述当前为真的内容」。

## 自检

- 只读前 5 行，能不能说出它管什么时刻、硬约束是什么？
- 每节篇幅与它对行为的影响成正比吗？
- 有没有和别的 rule / skill 重复的段落？
