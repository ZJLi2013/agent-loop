---
description: 什么时候该自己修、什么时候该停下问人。持有唯一一份中止清单，以及开跑前的两条命令与预算约束
alwaysApply: true
---

# 什么时候停

**遇到非预期情况不等于问人。** 本规则给出那条分界线，并持有**唯一一份中止清单**
（别处只留指针）。

它也约束执行中的偏离：`experiment-design` 的决策价值门只在开跑前问一次，
而偏离是一步步发生的——每一步都在「把某个数解释干净」，每一步看着都合理。

## 开跑前：写出两条具体命令

**结果为真时执行哪条命令，结果为假时执行哪条命令。** 两条都写出来，且它们不一样。

不接受的回答：加深理解、排除一个候选、确认某个猜测、为下一轮做准备。
排除候选只有在「排除之后那条命令确实换了」时才算数。

同一个问题连续两轮答案相同 → 这条线零信息，降优先级回 `task.md`，不再续。

## 预算：先声明，超了停

开跑前声明轮数与机时上限（默认 2 轮 / 2 小时 GPU）。超了**自动停并上报**，
不允许「再一轮就好」。沉没成本不是续的理由——跑完还要付读结果、写结论的 token。

## 每轮结束：报改变了哪条命令，不报读数

读数、候选集、solver 清单都不是产出，它们指向的那条「下一步命令变了」才是。
报不出来就明说这是零信息轮。

## 选项不能全是同一类

给用户的选择题里，若三个选项都是当前路径的不同变体，那就是把偏离包装成了选择。
至少留一个「停下来回主线」的选项。

## 先自修，再中止

先判断失败属于哪一类。前五类由 agent 自己处置完，才轮到中止清单：

| 失败长什么样 | 怎么走 |
|---|---|
| 连不上 / 找不到路径 / 容器不在 | **先重读 `facts.md` 并验证那几条**——这类失败里很大比例是事实过期，不是环境坏了 |
| 环境 / 瞬时：OOM、认证超时、缺依赖、端口占用、磁盘满、架构不匹配 | **自己修**，同类最多 2 次并计数；远端 GPU 的修法见 `remote-exec` 的「失败处置表」 |
| 契约不匹配：维数、单位、枚举名、帧数 | `experiment-design` 的「层 0 复现对方样例」 |
| **假设被推翻** | **这是结果不是故障。** 回填实验文档 → 回 `task.md` 重排优先级，继续 |
| 判据失效：代理指标判不出东西 | `experiment-design` 的「验收判据阶梯」，往任务指标那层退 |
| 上面都试过且重试上限用尽 | 查下面的硬性中止清单——**到这一步才轮到停下问人** |

自修要记进 `task.md` 的失败计数，否则分不清「第一次失败」和「已经试了三次」。
升级为 blocker 时带上已试过哪几条。

**重试必须换做法。** 同一个动作原样再跑一遍不算一次重试——它只会得到同一个错误，
白烧一轮预算。第二次要换方法、换工具、换库；还不行就退回去质疑前提，而不是加大力度。

## 硬性中止

任一命中即停，写 `## Auto-Stop Report`：当前位置、已试过什么、回主线的第一步。

1. 连续 2 轮结果无论正负都不改变主线
2. 连续 3 轮指标无改善或只在 noise 内波动
3. 连续 2 轮同一类环境错误且自修已用尽重试上限
4. 超预算（轮数或机时）
5. Phase 0 假设可能不成立，或发现主线其实没被这个问题卡住

另有三类与实验轮次无关、无论何时都要停下等人：**不可逆操作**（删数据、覆盖 checkpoint、
强推分支、长时间独占 GPU）、**Goal 说不清**（写不出一句话的「要解决什么问题」）、
**需要用户在有显著权衡的方案间选型**。
